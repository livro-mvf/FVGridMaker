// -----------------------------------------------------------------------------
// version.hpp - Auto-generated version information for FVGridMaker
// -----------------------------------------------------------------------------
// WARNING: This file is auto-generated by CMake. Do not edit manually!
// -----------------------------------------------------------------------------

#ifndef FVGRIDMAKER_VERSION_HPP
#define FVGRIDMAKER_VERSION_HPP

#include <string>
#include <cstdint>

namespace FVGridMaker {

/**
 * @struct VersionInfo
 * @brief Contains version information for the FVGridMaker library
 */
struct VersionInfo {
    // Semantic version components
    static constexpr uint32_t MAJOR = @FVG_PROJECT_VERSION_MAJOR@;
    static constexpr uint32_t MINOR = @FVG_PROJECT_VERSION_MINOR@;
    static constexpr uint32_t PATCH = @FVG_PROJECT_VERSION_PATCH@;
    
    // Git information
    static constexpr const char* GIT_HASH = "@FVG_GIT_HASH@";
    static constexpr const char* GIT_BRANCH = "@FVG_GIT_BRANCH@";
    static constexpr const char* GIT_DIRTY_SUFFIX = "@FVG_GIT_DIRTY_SUFFIX@";
    
    // Build information
    static constexpr const char* BUILD_TIMESTAMP = "@FVG_BUILD_TIMESTAMP@";
    static constexpr const char* BUILD_DATE = "@FVG_BUILD_DATE@";
    
    // Version strings
    static constexpr const char* SEMANTIC_VERSION = "@FVG_VERSION_SEMANTIC@";
    static constexpr const char* FULL_VERSION = "@FVG_VERSION_FULL@";
    
    /**
     * @brief Get the complete version string
     * @return String with full version information
     */
    static std::string toString() {
        return std::string(FULL_VERSION);
    }
    
    /**
     * @brief Get the semantic version string
     * @return String in format "MAJOR.MINOR.PATCH"
     */
    static std::string semanticString() {
        return std::string(SEMANTIC_VERSION);
    }
    
    /**
     * @brief Get version as a 32-bit integer (for comparisons)
     * @return Version encoded as 0xMMmmpp (Major, Minor, Patch)
     */
    static constexpr uint32_t toInteger() {
        return (MAJOR << 16) | (MINOR << 8) | PATCH;
    }
    
    /**
     * @brief Check if this is a development (dirty) build
     * @return true if there are uncommitted changes
     */
    static bool isDevelopmentBuild() {
        return std::string(GIT_DIRTY_SUFFIX) == "-dirty";
    }
    
    /**
     * @brief Check if this is a tagged release
     * @return true if built from a git tag
     */
    static bool isTaggedRelease() {
        // A tagged release doesn't have commit distance in description
        std::string version(FULL_VERSION);
        return version.find('+') != std::string::npos && 
               version.find('-') == std::string::npos;
    }
};

// Convenience namespace alias
namespace version = VersionInfo;

} // namespace FVGridMaker

#endif // FVGRIDMAKER_VERSION_HPP